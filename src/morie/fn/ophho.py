# morie.fn -- function file (rootcoder007/morie)
"""
Harris hawks optimizer spatial

Category: OptimSp
"""


def ophho(func=None, x0=None, bounds=None, n_dims=2, max_iter=100):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Harris hawks optimizer spatial

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.ophho.ophho is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "ophho"
alias = "ophho"
quote = "I think, therefore I am. -- Rene Descartes"
ophho = ophho


def cheatsheet() -> str:
    return "ophho({}) -> Harris hawks optimizer spatial"
