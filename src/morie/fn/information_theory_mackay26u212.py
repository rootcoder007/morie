"""Association expression (auto-extracted; see ref).."""

__all__ = ["information_theory_mackay_chapter_26_unnumbered_212"]


def information_theory_mackay_chapter_26_unnumbered_212(x, y=None):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.
    Association expression (auto-extracted; see ref).

    Formula: largest at d = 1 and d =K=S.

    Parameters
    ----------
    x : array-like
        Input data.

    Returns
    -------
    result : RichResult
        Inherits from ``dict`` (so ``isinstance(result, dict)`` is True
        and ``result["statistic"]`` / ``result.get(...)`` keep working),
        but also exposes a multi-section ``str(result)`` render. Keys: value.
        See ``morie.fn.describe('information_theory_mackay26u212')`` for the full guide.

    References
    ----------
    Information theory MacKay, ch.26 (unnumbered)
    """
    raise NotImplementedError(
        "morie.fn.information_theory_mackay26u212.information_theory_mackay_chapter_26_unnumbered_212 is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


def cheatsheet():
    return "information_theory_mackay26u212: Association expression (auto-extracted; see ref)."
