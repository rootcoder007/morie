# morie.fn -- function file (rootcoder007/morie)
"""
Depth interpolation bathymetry

Category: MarinSp
"""


def madpt(depth=None, temp=None, salinity=None, coords=None, n=50):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Depth interpolation bathymetry

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.madpt.madpt is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "madpt"
alias = "madpt"
quote = "The measure of a man is what he does with power. -- Plato"
madpt = madpt


def cheatsheet() -> str:
    return "madpt({}) -> Depth interpolation bathymetry"
