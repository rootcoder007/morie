"""
Voronoi diagram spatial partition

Category: GeoAnalysis
"""


def voroni(x=None, y=None, values=None, resolution=50):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Voronoi diagram spatial partition

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.voroni.voroni is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "voroni"
alias = "voroni"
quote = "An investment in knowledge pays the best interest. -- Benjamin Franklin"
voroni = voroni


def cheatsheet() -> str:
    return "voroni({}) -> Voronoi diagram spatial partition"
