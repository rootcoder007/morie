# morie.fn -- function file (rootcoder007/morie)
"""
Maternal mortality spatial

Category: GeoDem
"""


def gdmml(population=None, births=None, deaths=None, coords=None, n=50):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Maternal mortality spatial

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.gdmml.gdmml is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "gdmml"
alias = "gdmml"
quote = "No man ever steps in the same river twice. -- Heraclitus"
gdmml = gdmml


def cheatsheet() -> str:
    return "gdmml({}) -> Maternal mortality spatial"
