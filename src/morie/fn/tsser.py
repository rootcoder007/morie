"""
Spatial error regression temporal

Category: TempSpat
"""


def tsser(data=None, coords=None, times=None, n=50, t=10):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Spatial error regression temporal

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.tsser.tsser is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "tsser"
alias = "tsser"
quote = "The heart has its reasons of which reason knows nothing. -- Blaise Pascal"
tsser = tsser


def cheatsheet() -> str:
    return "tsser({}) -> Spatial error regression temporal"
