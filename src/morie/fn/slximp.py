"""SLX local/global impacts decomposition."""


def slximp(coef, theta):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    SLX local/global impacts decomposition.

    Category: SLX

    Parameters
    ----------
    coef, theta : see function signature.

    Returns
    -------
    SpatialResult
    """
    raise NotImplementedError(
        "morie.fn.slximp.slximp is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


slximp_fn = slximp


def cheatsheet() -> str:
    return "slximp({}) -> SLX local/global impacts decomposition."
