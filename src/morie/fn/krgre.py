# morie.fn -- function file (rootcoder007/morie)
"""
Kriging residual analysis

Category: KrigFilt
"""


def krgre(x=None, y=None, values=None, grid_size=20, range_param=30.0, sill=1.0, nugget=0.1):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Kriging residual analysis

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.krgre.krgre is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "krgre"
alias = "krgre"
quote = "Knowledge is power. -- Francis Bacon"
krgre = krgre


def cheatsheet() -> str:
    return "krgre({}) -> Kriging residual analysis"
