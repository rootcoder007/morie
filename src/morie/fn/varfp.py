"""
Variance filter spatial

Category: KrigFilt
"""


def varfp(x=None, y=None, values=None, grid_size=20, range_param=30.0, sill=1.0, nugget=0.1):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Variance filter spatial

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.varfp.varfp is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "varfp"
alias = "varfp"
quote = "The Analytical Engine weaves algebraic patterns. -- Ada Lovelace"
varfp = varfp


def cheatsheet() -> str:
    return "varfp({}) -> Variance filter spatial"
