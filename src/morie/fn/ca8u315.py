"""Correlation expression involving 'power' (auto-extracted; see reference for full context).."""

__all__ = ["ca_chapter_8_unnumbered_315"]


def ca_chapter_8_unnumbered_315(x, y=None):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.
    Correlation expression involving 'power' (auto-extracted; see reference for full context).

    Formula: power = NULL, + alternative= "greater")

    Parameters
    ----------
    x : array-like
        Input data.

    Returns
    -------
    result : RichResult
        Inherits from ``dict`` (so ``isinstance(result, dict)`` is True
        and ``result["statistic"]`` / ``result.get(...)`` keep working),
        but also exposes a multi-section ``str(result)`` render. Keys: value.
        See ``morie.fn.describe('ca8u315')`` for the full guide.

    References
    ----------
    Advanced Statistics in Criminology and Criminal Justice (Weisburd, Wilson, Wooditch & Britt, 5th ed, Springer 2022), ch.8 (unnumbered)
    """
    raise NotImplementedError(
        "morie.fn.ca8u315.ca_chapter_8_unnumbered_315 is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


def cheatsheet():
    return "ca8u315: Correlation expression involving 'power' (auto-extracted; see reference for full context)."
