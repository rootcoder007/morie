"""
Voronoi neighbor count distribution

Category: GeoAnalysis
"""


def vornnb(x=None, y=None, values=None, resolution=50):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Voronoi neighbor count distribution

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.vornnb.vornnb is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "vornnb"
alias = "vornnb"
quote = "We must know. We will know. -- David Hilbert"
vornnb = vornnb


def cheatsheet() -> str:
    return "vornnb({}) -> Voronoi neighbor count distribution"
