# morie.fn -- function file (rootcoder007/morie)
"""
Language distribution spatial

Category: GeoDem
"""


def gdlng(population=None, births=None, deaths=None, coords=None, n=50):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Language distribution spatial

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.gdlng.gdlng is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "gdlng"
alias = "gdlng"
quote = "Give me a place to stand and I will move the earth. -- Archimedes"
gdlng = gdlng


def cheatsheet() -> str:
    return "gdlng({}) -> Language distribution spatial"
