"""Probability equation extracted from BookAdvanced elementsofstatisticallearning.."""

__all__ = ["bookadvanced_elementsofstatisticallearning_chapter_7_equation_37"]


def bookadvanced_elementsofstatisticallearning_chapter_7_equation_37(x, cdf=None):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.
    Probability equation extracted from BookAdvanced elementsofstatisticallearning.

    Formula: Pr(Z|θm,Mm)Pr(θm|Mm)dθm

    Parameters
    ----------
    x : array-like
        Input data.
    cdf : array-like
        Input data.

    Returns
    -------
    result : RichResult
        Inherits from ``dict`` (so ``isinstance(result, dict)`` is True
        and ``result["statistic"]`` / ``result.get(...)`` keep working),
        but also exposes a multi-section ``str(result)`` render. Keys: value.
        See ``morie.fn.describe('bookadvanced_elementsofstatisticallearning7e37')`` for the full guide.

    References
    ----------
    BookAdvanced elementsofstatisticallearning, ch.7 eq.7.37
    """
    raise NotImplementedError(
        "morie.fn.bookadvanced_elementsofstatisticallearning7e37.bookadvanced_elementsofstatisticallearning_chapter_7_equation_37 is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


def cheatsheet():
    return "bookadvanced_elementsofstatisticallearning7e37: Probability equation extracted from BookAdvanced elementsofstatisticallearning."
