# morie.fn -- function file (rootcoder007/morie)
"""
Intensity estimation (adaptive)

Category: PointProc
"""


def ppint2(points=None, n=80, window=(0, 1, 0, 1), intensity=None):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Intensity estimation (adaptive)

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.ppint2.ppint2 is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "ppint2"
alias = "ppint2"
quote = "You have power over your mind, not outside events. -- Marcus Aurelius"
ppint2 = ppint2


def cheatsheet() -> str:
    return "ppint2({}) -> Intensity estimation (adaptive)"
