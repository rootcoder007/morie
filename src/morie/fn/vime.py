"""Empirical orthogonal functions (EOFs)."""

__all__ = ["empirical_orthogonal_func"]


def empirical_orthogonal_func(X):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.
    Empirical orthogonal functions (EOFs)

    Formula: PCA on space-time field

    Parameters
    ----------
    X : array-like
        Input data.

    Returns
    -------
    result : dict
        Keys: estimate

    References
    ----------
    Lorenz (1956)
    """
    raise NotImplementedError(
        "morie.fn.vime.empirical_orthogonal_func is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


def cheatsheet():
    return "vime: Empirical orthogonal functions (EOFs)"
