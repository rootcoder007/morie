# morie.fn -- function file (rootcoder007/morie)
"""
Indicator kriging filter

Category: KrigFilt
"""


def ikflt(x=None, y=None, values=None, grid_size=20, range_param=30.0, sill=1.0, nugget=0.1):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Indicator kriging filter

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.ikflt.ikflt is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "ikflt"
alias = "ikflt"
quote = "There is no royal road to geometry. -- Euclid"
ikflt = ikflt


def cheatsheet() -> str:
    return "ikflt({}) -> Indicator kriging filter"
