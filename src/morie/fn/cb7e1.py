"""GeneralStatistics expression involving 'factorial' (auto-extracted; see reference for full context).."""

__all__ = ["cb_chapter_7_equation_1"]


def cb_chapter_7_equation_1(x, cdf=None):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.
    GeneralStatistics expression involving 'factorial' (auto-extracted; see reference for full context).

    Formula: [EQ]  = −()

    Parameters
    ----------
    x : array-like
        Input data.
    cdf : array-like
        Input data.

    Returns
    -------
    result : RichResult
        Inherits from ``dict`` (so ``isinstance(result, dict)`` is True
        and ``result["statistic"]`` / ``result.get(...)`` keep working),
        but also exposes a multi-section ``str(result)`` render. Keys: value.
        See ``morie.fn.describe('cb7e1')`` for the full guide.

    References
    ----------
    Beginner's Guide to Statistics for Criminology and Criminal Justice using R (Wooditch et al., Springer 2021), ch.7 eq.7.1
    """
    raise NotImplementedError(
        "morie.fn.cb7e1.cb_chapter_7_equation_1 is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


def cheatsheet():
    return "cb7e1: GeneralStatistics expression involving 'factorial' (auto-extracted; see reference for full context)."
