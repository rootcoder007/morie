# morie.fn -- function file (rootcoder007/morie)
"""
Collocated co-kriging

Category: KrigFilt
"""


def ckflm(x=None, y=None, values=None, grid_size=20, range_param=30.0, sill=1.0, nugget=0.1):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Collocated co-kriging

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.ckflm.ckflm is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "ckflm"
alias = "ckflm"
quote = "An investment in knowledge pays the best interest. -- Benjamin Franklin"
ckflm = ckflm


def cheatsheet() -> str:
    return "ckflm({}) -> Collocated co-kriging"
