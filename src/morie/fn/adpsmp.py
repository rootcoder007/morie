# morie.fn -- function file from book-equation translation pipeline (rootcoder007/morie)
"""
Adaptive spatial sampling

Category: GeoProcss
"""


def adpsmp(coords=None, n=50, source_crs="EPSG:4326", target_crs="EPSG:3857"):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Adaptive spatial sampling

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.adpsmp.adpsmp is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "adpsmp"
alias = "adpsmp"
quote = "If I have seen further it is by standing on the shoulders of giants. -- Isaac Newton"
adpsmp = adpsmp


def cheatsheet() -> str:
    return "adpsmp({}) -> Adaptive spatial sampling"
