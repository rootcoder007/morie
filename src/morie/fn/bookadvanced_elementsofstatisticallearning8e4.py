"""Resampling equation extracted from BookAdvanced elementsofstatisticallearning.."""

__all__ = ["bookadvanced_elementsofstatisticallearning_chapter_8_equation_4"]


def bookadvanced_elementsofstatisticallearning_chapter_8_equation_4(x, cdf=None):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.
    Resampling equation extracted from BookAdvanced elementsofstatisticallearning.

    Formula: [EQ] ˆse[ˆµ(x)] = [h(x)T (HT H)− 1h(x)]

    Parameters
    ----------
    x : array-like
        Input data.
    cdf : array-like
        Input data.

    Returns
    -------
    result : RichResult
        Inherits from ``dict`` (so ``isinstance(result, dict)`` is True
        and ``result["statistic"]`` / ``result.get(...)`` keep working),
        but also exposes a multi-section ``str(result)`` render. Keys: value.
        See ``morie.fn.describe('bookadvanced_elementsofstatisticallearning8e4')`` for the full guide.

    References
    ----------
    BookAdvanced elementsofstatisticallearning, ch.8 eq.8.4
    """
    raise NotImplementedError(
        "morie.fn.bookadvanced_elementsofstatisticallearning8e4.bookadvanced_elementsofstatisticallearning_chapter_8_equation_4 is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


def cheatsheet():
    return "bookadvanced_elementsofstatisticallearning8e4: Resampling equation extracted from BookAdvanced elementsofstatisticallearning."
