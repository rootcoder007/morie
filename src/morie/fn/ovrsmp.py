# morie.fn -- function file (rootcoder007/morie)
"""
Oversampling rare spatial events

Category: GeoProcss
"""


def ovrsmp(coords=None, n=50, source_crs="EPSG:4326", target_crs="EPSG:3857"):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Oversampling rare spatial events

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.ovrsmp.ovrsmp is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "ovrsmp"
alias = "ovrsmp"
quote = "In the midst of chaos, there is also opportunity. -- Sun Tzu"
ovrsmp = ovrsmp


def cheatsheet() -> str:
    return "ovrsmp({}) -> Oversampling rare spatial events"
