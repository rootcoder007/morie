"""GeneralStatistics equation extracted from BookAdvanced elementsofstatisticallearning.."""

__all__ = ["bookadvanced_elementsofstatisticallearning_chapter_10_equation_21"]


def bookadvanced_elementsofstatisticallearning_chapter_10_equation_21(x, cdf=None):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.
    GeneralStatistics equation extracted from BookAdvanced elementsofstatisticallearning.

    Formula: [EQ] fk(X) = log pk(X)− 1

    Parameters
    ----------
    x : array-like
        Input data.
    cdf : array-like
        Input data.

    Returns
    -------
    result : RichResult
        Inherits from ``dict`` (so ``isinstance(result, dict)`` is True
        and ``result["statistic"]`` / ``result.get(...)`` keep working),
        but also exposes a multi-section ``str(result)`` render. Keys: value.
        See ``morie.fn.describe('bookadvanced_elementsofstatisticallearning10e21')`` for the full guide.

    References
    ----------
    BookAdvanced elementsofstatisticallearning, ch.10 eq.10.21
    """
    raise NotImplementedError(
        "morie.fn.bookadvanced_elementsofstatisticallearning10e21.bookadvanced_elementsofstatisticallearning_chapter_10_equation_21 is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


def cheatsheet():
    return "bookadvanced_elementsofstatisticallearning10e21: GeneralStatistics equation extracted from BookAdvanced elementsofstatisticallearning."
