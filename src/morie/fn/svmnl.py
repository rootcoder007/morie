"""Multinomial spatial choice model"""


def multinomial_spatial(data, *, method="default"):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Multinomial spatial choice model

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.svmnl.multinomial_spatial is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


mult = multinomial_spatial


def cheatsheet() -> str:
    return "multinomial_spatial({}) -> Multinomial spatial choice model"
