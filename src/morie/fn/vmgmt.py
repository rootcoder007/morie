"""
Geometric anisotropy transform

Category: Variogram
"""


def vmgmt(x=None, y=None, values=None, n_lags=15, max_lag=None):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Geometric anisotropy transform

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.vmgmt.vmgmt is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "vmgmt"
alias = "vmgmt"
quote = "Measure what is measurable, and make measurable what is not. -- Galileo Galilei"
vmgmt = vmgmt


def cheatsheet() -> str:
    return "vmgmt({}) -> Geometric anisotropy transform"
