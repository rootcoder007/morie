# morie.fn -- function file (rootcoder007/morie)
"""
Ant colony optimization spatial

Category: OptimSp
"""


def opaco(func=None, x0=None, bounds=None, n_dims=2, max_iter=100):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Ant colony optimization spatial

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.opaco.opaco is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "opaco"
alias = "opaco"
quote = "To understand God's thoughts we must study statistics. -- Florence Nightingale"
opaco = opaco


def cheatsheet() -> str:
    return "opaco({}) -> Ant colony optimization spatial"
