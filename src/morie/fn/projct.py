# morie.fn -- function file (rootcoder007/morie)
"""
Coordinate projection transform

Category: GeoProcss
"""


def projct(coords=None, n=50, source_crs="EPSG:4326", target_crs="EPSG:3857"):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Coordinate projection transform

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.projct.projct is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "projct"
alias = "projct"
quote = "Mathematics is the queen of the sciences. -- Carl Friedrich Gauss"
projct = projct


def cheatsheet() -> str:
    return "projct({}) -> Coordinate projection transform"
