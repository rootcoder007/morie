# morie.fn -- function file (rootcoder007/morie)
"""
Sine cosine algorithm spatial

Category: OptimSp
"""


def opsca(func=None, x0=None, bounds=None, n_dims=2, max_iter=100):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Sine cosine algorithm spatial

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.opsca.opsca is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "opsca"
alias = "opsca"
quote = "Number rules the universe. -- Pythagoras"
opsca = opsca


def cheatsheet() -> str:
    return "opsca({}) -> Sine cosine algorithm spatial"
