# morie.fn -- function file (rootcoder007/morie)
"""
DBSCAN spatial regionalization

Category: OptimSp
"""


def opdbr(func=None, x0=None, bounds=None, n_dims=2, max_iter=100):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    DBSCAN spatial regionalization

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.opdbr.opdbr is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "opdbr"
alias = "opdbr"
quote = "What is now proved was once only imagined. -- William Blake"
opdbr = opdbr


def cheatsheet() -> str:
    return "opdbr({}) -> DBSCAN spatial regionalization"
