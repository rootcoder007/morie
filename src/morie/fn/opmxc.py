# morie.fn -- function file (rootcoder007/morie)
"""
Spatial max covering location

Category: OptimSp
"""


def opmxc(func=None, x0=None, bounds=None, n_dims=2, max_iter=100):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Spatial max covering location

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.opmxc.opmxc is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "opmxc"
alias = "opmxc"
quote = "If I have seen further it is by standing on the shoulders of giants. -- Isaac Newton"
opmxc = opmxc


def cheatsheet() -> str:
    return "opmxc({}) -> Spatial max covering location"
