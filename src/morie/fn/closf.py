# morie.fn -- function file (rootcoder007/morie)
"""
Closing morphological

Category: KrigFilt
"""


def closf(x=None, y=None, values=None, grid_size=20, range_param=30.0, sill=1.0, nugget=0.1):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Closing morphological

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.closf.closf is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "closf"
alias = "closf"
quote = "The whole is greater than the sum of its parts. -- Aristotle"
closf = closf


def cheatsheet() -> str:
    return "closf({}) -> Closing morphological"
