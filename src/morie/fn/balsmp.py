# morie.fn -- function file from book-equation translation pipeline (rootcoder007/morie)
"""
Balanced spatial sampling

Category: GeoProcss
"""


def balsmp(coords=None, n=50, source_crs="EPSG:4326", target_crs="EPSG:3857"):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Balanced spatial sampling

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.balsmp.balsmp is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "balsmp"
alias = "balsmp"
quote = "There is no royal road to geometry. -- Euclid"
balsmp = balsmp


def cheatsheet() -> str:
    return "balsmp({}) -> Balanced spatial sampling"
