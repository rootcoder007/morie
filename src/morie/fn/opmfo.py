# morie.fn -- function file (rootcoder007/morie)
"""
Moth-flame optimizer spatial

Category: OptimSp
"""


def opmfo(func=None, x0=None, bounds=None, n_dims=2, max_iter=100):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Moth-flame optimizer spatial

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.opmfo.opmfo is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "opmfo"
alias = "opmfo"
quote = "No man ever steps in the same river twice. -- Heraclitus"
opmfo = opmfo


def cheatsheet() -> str:
    return "opmfo({}) -> Moth-flame optimizer spatial"
