# morie.fn -- function file (rootcoder007/morie)
"""
Dilation spatial operation

Category: KrigFilt
"""


def dilat(x=None, y=None, values=None, grid_size=20, range_param=30.0, sill=1.0, nugget=0.1):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Dilation spatial operation

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.dilat.dilat is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "dilat"
alias = "dilat"
quote = "Mathematics is the art of giving the same name to different things. -- Henri Poincare"
dilat = dilat


def cheatsheet() -> str:
    return "dilat({}) -> Dilation spatial operation"
