# morie.fn -- function file (rootcoder007/morie)
"""
Convex hull boundary computation

Category: GeoAnalysis
"""


def convhl(x=None, y=None, values=None, resolution=50):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.

    Convex hull boundary computation

    Returns
    -------
    DescriptiveResult
    """
    raise NotImplementedError(
        "morie.fn.convhl.convhl is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


short = "convhl"
alias = "convhl"
quote = "It does not matter how slowly you go as long as you do not stop. -- Confucius"
convhl = convhl


def cheatsheet() -> str:
    return "convhl({}) -> Convex hull boundary computation"
