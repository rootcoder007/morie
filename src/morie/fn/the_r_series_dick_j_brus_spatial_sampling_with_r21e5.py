"""Correlation equation extracted from [The R Series] Dick J. Brus - Spatial Sampling with R.."""

__all__ = ["the_r_series_dick_j_brus_spatial_sampling_with_r_chapter_21_equation_5"]


def the_r_series_dick_j_brus_spatial_sampling_with_r_chapter_21_equation_5(x, y=None):
    """NOT IMPLEMENTED. The previous body of this callable did not compute the
    method named here (it returned a placeholder such as the mean of its
    input, or a statistic of internally generated data). It raises
    NotImplementedError until the real method is built.
    Correlation equation extracted from [The R Series] Dick J. Brus - Spatial Sampling with R.

    Formula: vgmodel <- vgm(model = "Exp", psill = thetas[

    Parameters
    ----------
    x : array-like
        Input data.

    Returns
    -------
    result : RichResult
        Inherits from ``dict`` (so ``isinstance(result, dict)`` is True
        and ``result["statistic"]`` / ``result.get(...)`` keep working),
        but also exposes a multi-section ``str(result)`` render. Keys: value.
        See ``morie.fn.describe('the_r_series_dick_j_brus_spatial_sampling_with_r21e5')`` for the full guide.

    References
    ----------
    [The R Series] Dick J. Brus - Spatial Sampling with R, ch.21 eq.21.5
    """
    raise NotImplementedError(
        "morie.fn.the_r_series_dick_j_brus_spatial_sampling_with_r21e5.the_r_series_dick_j_brus_spatial_sampling_with_r_chapter_21_equation_5 is not implemented yet: its former body returned a "
        "placeholder, not the method its name and docstring describe."
    )


def cheatsheet():
    return "the_r_series_dick_j_brus_spatial_sampling_with_r21e5: Correlation equation extracted from [The R Series] Dick J. Brus - Spatial Sampling with R."
