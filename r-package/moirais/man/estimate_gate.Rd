% Generated by moirais generate_rd.py
\name{estimate_gate}
\alias{estimate_gate}
\title{'ci_lower', 'ci_upper', 'n'.}
\description{
  Estimate Group Average Treatment Effects (GATE)

Applies AIPW within each level of 'group_col' to estimate
stratum-specific treatment effects.
}
\usage{
  estimate_gate(group_col)
}
\arguments{
  \item{group_col}{Name of the grouping variable (e.g. '"gender"').}
}
\value{
  Data frame with columns: 'group', 'ate', 'se',
}
\examples{
  set.seed(3)
  df <- data.frame(
  t = rbinom(300, 1, 0.4),
  y = rnorm(300),
  x = rnorm(300),
  g = sample(c("A", "B"), 300, replace = TRUE)
  )
  estimate_gate(df, "t", "y", "x", "g")
}
