% Generated by moirais generate_rd.py
\name{moirais_load_cpads}
\alias{moirais_load_cpads}
\title{Load CPADS data: local files -> cache -> CKAN API}
\description{
  Load CPADS data using a resolution chain:
  \enumerate{
    \item Local RDS/CSV files in standard project locations
    \item SQLite cache (\code{data/cache/moirais.db})
    \item CKAN API fetch (requires internet)
  }
}
\usage{
  moirais_load_cpads(db_path = NULL, use_ckan = TRUE)
}
\arguments{
  \item{db_path}{Optional override for the database path.}
  \item{use_ckan}{Logical; if TRUE and data not found locally or in cache,
    attempt to fetch from the CKAN API.}
}
\value{
  A data.frame with canonical CPADS columns.
}
