% Generated by moirais generate_rd.py
\name{moirais_load_dataset}
\alias{moirais_load_dataset}
\title{Load a dataset by catalog key}
\description{
  Load a dataset by its catalog key using the resolution chain:
  built-in SQLite database -> user cache -> local file ingest -> CKAN API -> error.

  Supports fuzzy matching: \code{moirais_load_dataset("cpads_2021")} resolves
  to \code{opencanada_cpads_2021}.
}
\usage{
  moirais_load_dataset(key, db_path = NULL)
}
\arguments{
  \item{key}{Dataset catalog key (or fuzzy match).}
  \item{db_path}{Optional override for the database path.}
}
\value{
  A data.frame.
}
