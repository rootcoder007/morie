% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset_portal_catalog.R
\name{morie_dataset_portal_catalog}
\alias{morie_dataset_portal_catalog}
\title{Unified cross-portal catalog of every morie-shippable dataset}
\usage{
morie_dataset_portal_catalog(portal = NULL)
}
\arguments{
\item{portal}{Optional character filter: \code{"chicago"}, \code{"nyc_nypd"},
\code{"nyc_opendata"}, \code{"tps_arcgis_hub"}, \code{"tps_psdp"},
\code{"ontario_ckan"}, \code{"vancouver_opendata"}. \code{NULL} (default)
returns all portals.}
}
\value{
A \code{data.frame} with one row per dataset. Columns:
\code{dataset_key}, \code{source}, \code{id}, \code{api_modes}, \code{loader},
\code{dict_url}, \code{n_rows_bundled}.
}
\description{
Phase 3CCC4. Single-view aggregator over morie's per-portal
registries. Returns one row per (portal, dataset) pair with a
uniform schema so callers can answer questions like "which TPS
datasets does morie ship?" or "what fixtures are bundled for
Chicago?" without knowing each portal's registry shape.
}
\details{
API mode column legend (\code{api_modes}):
\itemize{
\item \strong{soda2}        -- Socrata SODA2 \verb{/resource/<id>.json} (open).
\item \strong{soda2_csv}    -- Socrata SODA2 \verb{/resource/<id>.csv} (open).
\item \strong{soda2_geojson}-- Socrata SODA2 \verb{/resource/<id>.geojson} (open).
\item \strong{soda3}        -- Socrata SODA3
\verb{/api/v3/views/<id>/query.json} or \code{.csv} (requires
authentication; see Socrata support ref below).
\item \strong{odata}        -- Socrata OData v4
\verb{/api/odata/v4/<id>} (open, but \verb{$filter} is broken on many
Socrata datasets due to Edm typing -- prefer soda3 for filters).
\item \strong{arcgis_rest}  -- ArcGIS FeatureServer REST query
endpoint (open).
\item \strong{arcgis_hub}   -- TPS Hub OGC-API-Features (open).
\item \strong{ckan}         -- CKAN datastore (open).
\item \strong{opendatasoft_v21} -- Opendatasoft Explore API v2.1
\verb{/api/explore/v2.1/catalog/datasets/<id>/records} (open).
}
}
\note{
SODA3 (\verb{/api/v3/views/<id>/query.\{json,csv\}}) requires
authentication per Socrata's API documentation
(\url{https://support.socrata.com/hc/en-us/articles/34730618169623-SODA3-API}).
morie's \code{mode = "soda3"} accepts an \code{app_token} argument on
every wrapper that supports it.
}
\examples{
cat_df <- morie_dataset_portal_catalog()
nrow(cat_df)
table(cat_df$source)

tps <- morie_dataset_portal_catalog(portal = "tps_arcgis_hub")
head(tps$dataset_key)
}
