% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets_vpd.R
\name{morie_datasets_vpd_crime}
\alias{morie_datasets_vpd_crime}
\title{Load Vancouver Police Department crime incident data}
\usage{
morie_datasets_vpd_crime(
  offline = TRUE,
  zip_path = NULL,
  csv_path = NULL,
  max_features = NULL,
  accept_terms = FALSE
)
}
\arguments{
\item{offline}{If \code{TRUE} (default) and \code{zip_path}/\code{csv_path} are
\code{NULL}, reads the bundled 550-row sample.}

\item{zip_path}{Optional path to a user-downloaded
\code{crimedata_csv_AllNeighbourhoods_AllYears.zip}. Mutually
exclusive with \code{csv_path}.}

\item{csv_path}{Optional path to a pre-extracted CSV. Mutually
exclusive with \code{zip_path}.}

\item{max_features}{Optional row cap.}

\item{accept_terms}{If reading from a user-supplied zip/csv,
pass \code{TRUE} to silently acknowledge VPD's terms (else a
warning surfaces them once per session).}
}
\value{
A \code{data.frame} with 10 columns.
}
\description{
Phase 3DDD2. Loads VPD's open crime incident records. Three
source modes:
}
\details{
\describe{
\item{\code{offline = TRUE} (default)}{Reads a bundled stratified
550-row sample (50 rows per \code{TYPE} x 11 categories) covering
years 2003-2026 and all 25 VPD-defined neighbourhoods.
Intended for tests + intro examples -- NOT for analysis.}
\item{\code{zip_path = "..."}}{Reads from a local copy of VPD's
\code{crimedata_csv_AllNeighbourhoods_AllYears.zip} that the
caller has downloaded themselves from
\url{https://geodash.vpd.ca/opendata/} (after accepting
VPD's terms + conditions there).}
\item{\code{csv_path = "..."}}{Reads from a pre-extracted CSV
(skip the zip if the caller already has the CSV on disk).}
}

The bundled sample is open-licensed under VPD's GeoDASH terms;
the full feed requires manual T&C acceptance per VPD policy and
there is no automation-friendly API. See
\code{\link[=morie_datasets_vpd_legal_disclaimer]{morie_datasets_vpd_legal_disclaimer()}} for the full text.

Columns (10): \code{TYPE}, \code{YEAR}, \code{MONTH}, \code{DAY}, \code{HOUR}, \code{MINUTE},
\code{HUNDRED_BLOCK}, \code{NEIGHBOURHOOD}, \code{X}, \code{Y}. Coordinates are UTM
Zone 10 N (NAD83 / EPSG:26910). For Offence-Against-a-Person
incidents the location is deliberately randomized + offset per
VPD's privacy policy.

Categories present in the sample:
\itemize{
\item Break and Enter Commercial
\item Break and Enter Residential/Other
\item Homicide
\item Mischief
\item Offence Against a Person (aggregated)
\item Other Theft
\item Theft from Vehicle
\item Theft of Bicycle
\item Theft of Vehicle
\item Vehicle Collision or Pedestrian Struck (with Fatality)
\item Vehicle Collision or Pedestrian Struck (with Injury)
}
}
\examples{
df <- morie_datasets_vpd_crime(offline = TRUE)
nrow(df)              # 550
table(df$TYPE)
table(df$NEIGHBOURHOOD)
}
\references{
VPD GeoDASH Open Data,
\url{https://geodash.vpd.ca/opendata/}.
}
