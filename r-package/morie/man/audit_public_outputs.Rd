% Generated by morie generate_rd.py
\name{audit_public_outputs}
\alias{audit_public_outputs}
\title{Audit declared outputs against files on disk}
\description{
  Audit declared outputs against files on disk
}
\usage{
  audit_public_outputs(project_root, manifest)
}
\arguments{
  \item{project_root}{Project root directory.}
  \item{manifest}{Manifest data frame. If 'NULL', loaded from disk.}
}
\value{
  Data frame containing declared and observed output status.
}
\examples{
\dontrun{
  # Walks the project's outputs_manifest.csv and confirms every
  # declared public artefact exists on disk.
  manifest <- read_outputs_manifest(project_root = ".")
  audit <- audit_public_outputs(project_root = ".",
                                manifest = manifest)
  summarize_output_audit(audit)
}
}
