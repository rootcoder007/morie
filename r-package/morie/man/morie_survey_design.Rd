% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey.R
\name{morie_survey_design}
\alias{morie_survey_design}
\title{Construct a survey design object}
\usage{
morie_survey_design(
  data,
  weights_col,
  strata_col = NULL,
  cluster_col = NULL,
  fpc_col = NULL,
  nest = FALSE
)
}
\arguments{
\item{data}{data.frame.}

\item{weights_col}{Column name of analytic/probability weights.}

\item{strata_col}{Optional strata column.}

\item{cluster_col}{Optional PSU/cluster column.}

\item{fpc_col}{Optional finite-population-correction column.}

\item{nest}{If TRUE, treat cluster IDs as nested within strata.}
}
\value{
A \code{survey::svydesign} object when the \pkg{survey}
package is installed; otherwise an S3 list of class
\code{morie_survey_design_fallback} carrying \code{data},
\code{weights}, optional \code{strata}, and optional
\code{cluster}.
}
\description{
Returns a \code{survey::svydesign} object when \code{survey} is available; otherwise
returns a lightweight list with the same fields the morie helpers consume.
}
\examples{
.make_survey_df <- function(n = 100L, seed = 1L) {
    set.seed(seed)
    data.frame(id = seq_len(n), y = rnorm(n, 10, 2), x = rnorm(n, 
        5, 1), w = runif(n, 0.5, 1.5), s = sample(c("A", "B"), 
        n, replace = TRUE), cl = sample(seq_len(10), n, replace = TRUE), 
        fpc = rep(1000L, n))
}
df <- .make_survey_df()
morie_survey_design(df, weights_col = "w")
}
