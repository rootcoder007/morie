# The shared numeric core (morie_core.h) uses C++17 features.
CXX_STD = CXX17
# The SIU scraper (siu_scrape.cpp) links libcurl for HTTP(S).
PKG_CPPFLAGS = $(shell curl-config --cflags 2>/dev/null)
PKG_LIBS = $(shell curl-config --libs 2>/dev/null || echo -lcurl)
